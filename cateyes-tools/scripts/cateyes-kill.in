#!@PYTHON@

from os.path import dirname, join, realpath
import sys
sys.path.insert(1, join(dirname(dirname(realpath(__file__))), '@pythondir@'))

import cateyes_tools.kill

if __name__ == '__main__':
    cateyes_tools.kill.main()
